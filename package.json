{
  "name": "coburn-clock-in",
  "version": "1.0.0",
  "private": true,
  "description": "Coburn Equipment Rentals time clock — Firebase web app with location verification",
  "scripts": {
    "deploy": "./deploy.sh",
    "deploy:web": "./deploy.sh --hosting-only",
    "setup": "npm --prefix functions install && npm --prefix web install",
    "build": "npm --prefix functions run build && npm --prefix web run build",
    "emulators": "firebase emulators:start --project demo-clockin",
    "test": "npm --prefix web run test:api",
    "test:ui": "npm --prefix web run test:ui"
  },
  "engines": {
    "node": ">=20"
  }
}
