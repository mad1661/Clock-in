/// <reference types="vite/client" />

/** Stamped in at build time by vite.config.ts. */
declare const __BUILD_ID__: string;
/** The version from web/package.json, stamped in the same way. */
declare const __APP_VERSION__: string;
