# Loaded by the Firebase CLI ONLY when running the local emulator suite.
# It is never uploaded on deploy, so nothing here affects production.
#
# The 30-second minimum between punches exists to stop double-taps and scripted
# hammering. It makes the e2e suite sleep for no reason, so the emulator runs
# with it disabled.
MIN_SECONDS_BETWEEN_ACTIONS=0
